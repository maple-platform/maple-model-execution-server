# Official DeepSeeNet reference environment: Python 3.6, CPU or GPU
# (upstream README: "python =3.6 / tensorflow >=1.6.0 / keras =2.2.4 / Linux").
# Trimmed to inference-only needs (drops upstream's opencv-python/sklearn/pytest,
# which are only used by its training/eval scripts, not by this package).
# tensorflow is unpinned upstream; pinned here to the last TF1 release that
# still ships Python 3.6 wheels and matches keras==2.2.4's TF1-era API.
# Maple administrator: confirm a Python 3.6 wheel is available for this runtime
# image; this stack predates the other registered Ophthalmology models by
# several TensorFlow major versions.
#
# NOTE: upstream's own requirements.txt pins numpy==1.21 and Pillow==9.1.0,
# but both dropped Python 3.6 support before those versions (numpy at 1.20,
# Pillow at 9.0) -- neither pin can actually install under the Python 3.6
# upstream itself requires. numpy is further constrained by tensorflow==1.15.5
# itself, which declares numpy>=1.16.0,<1.19.0. Pinned below to the version
# verified locally against this exact stack.
numpy==1.18.5
tensorflow==1.15.5
keras==2.2.4
pillow==8.4.0
pandas==1.1.5

# Not pinned upstream: unconstrained "protobuf>=3.6.1" resolves to a modern
# protobuf that requires Python >=3.7, which breaks installation under the
# Python 3.6 this stack requires. Pinned to the last release supporting
# Python 3.6, verified locally against this exact tensorflow==1.15.5 stack.
protobuf==3.19.6
